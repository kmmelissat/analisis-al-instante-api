# Core FastAPI dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6
python-dotenv>=1.0.0

# HTTP client
httpx>=0.25.0

# Data processing (using more compatible versions)
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0
openpyxl>=3.1.0
xlrd>=2.0.0

# AI integration
openai>=1.3.0

# File handling
aiofiles>=23.0.0

# Optional dependencies (removed problematic ones for deployment)
# python-jose[cryptography]==3.3.0  # Only needed for auth
# passlib[bcrypt]==1.7.4            # Only needed for auth  
# python-magic==0.4.27              # Can cause deployment issues
# pytest==7.4.3                     # Dev dependency
# pytest-asyncio==0.21.1            # Dev dependency
